

name: 09 - Using Functions

on:

  push:
    branches: [ "main" ]

  workflow_dispatch:
    

jobs:
  build:
  
    runs-on: ubuntu-latest
    
    - name: failing step
      run: exit 1

    
    
    
